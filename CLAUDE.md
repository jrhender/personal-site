# CLAUDE.md

## Workflow
- Always commit changes to the repo after completing them.
- To create GitHub PRs, use `GH_TOKEN="proxy-managed" gh pr create --base main ...` — the proxy injects real credentials automatically.
